<!--帧布局等同于andorid上的FrameLayout-->
<FrameLayout dataTag="frameLayout" layoutWidth="match_parent" layoutHeight="wrap_content">

    <VHLayout action="${action}" background="${bgColor}" flag="flag_exposure|flag_clickable"
        layoutHeight="wrap_content" layoutWidth="match_parent" orientation="V">

        <NImage layoutGravity="h_center" layoutHeight="72rp" layoutMarginTop="10rp"
            layoutWidth="72rp" src="${imageUrl}" />

        <NText layoutGravity="h_center" layoutHeight="wrap_content" layoutMarginBottom="10rp"
            layoutMarginTop="10rp" layoutWidth="wrap_content" textColor="${textColor}"
            textSize="${textSize}" text="${text}" />
    </VHLayout>

</FrameLayout>